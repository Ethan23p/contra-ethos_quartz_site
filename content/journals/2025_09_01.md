- If I try really hard I might - just might - get my website to a 'minimum viable product' state today! It's super close, I just need to:
	- Nah, this no longer falls under the umbrella of 'minimum': "Remedy a couple artifacts of transitioning from Logseq"
	  collapsed:: true
		- solution: Never use a heading with children as the first line of the page; precede with something else, a title is often appropriate. Logseq assumes that a heading in the very first line is a title, thus doesn't make it a top level list item, thus messing up the formatting for any children blocks. Problem: ~~In some circumstances some lines in my Markdown files are interpreted as code blocks via Markdown.~~
		  collapsed:: true
			- context:
			  collapsed:: true
				- I'm writing content in Logseq; it interprets and stores the content I write in the app as Markdown files.
				- The Markdown rule being evoked, I believe, is: 4 consecutive spaces = code block
				- I've provided an example file "Agent Dashboard.md"
				  collapsed:: true
					- In this file you can see that, when Logseq interprets and stores the content, it doesn't interpret that first h2 as a top-level list item which enables the following lines to be a code block.
					- I've also provided a jpg image of what that page looks like when rendered
			- some circumstances:
			  collapsed:: true
				- the first line is an h2 (in Markdown terms, like this: '## h2')
				- there is frequently four consecutive spaces in Logseq's Markdown; typically text avoids becoming a code block by being technically identified as a list item due to the preceding `-` symbol.
				  collapsed:: true
					- How does Markdown conventionally define formatting for:
					  collapsed:: true
						- continuous, multi-tiered lists
						- various different lists with multiple tiers
						- code blocks - I've heard it's with indentation
						- bonus: how does Obsidian (when it interprets and stores content as Markdown files) specify these things?
				- there's no actual frontmatter
				  collapsed:: true
					- if there is frontmatter, Logseq properly interprets the first line, also an h2, as a list item
			- Our objective:
			  collapsed:: true
				- Identify the particular issue and think of multiple potential approaches to fixing this issue through conventional means in this framework.
				- **Do not** proceed to writing or modifying code; let's discuss and then I can let you know which route we will take.
		- Logseq uses a different method of specifying frontmatter than Obsidian, so it needs to be automatically transformed into that other form
		- Logseq, of course, renders everything as an HTML list; this is almost bearable, but something should be done
	- redirect my domain to the new site
	- create a profile on UpWork